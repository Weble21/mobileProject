<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_start"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project"
        tools:targetApi="34">

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="omn4728sj9"
            />

        <activity
            android:name=".Disaster_message"
            android:exported="false"
            android:label="Add disaster message"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Project"></activity>
        <activity
            android:name=".Shelter_list"
            android:exported="false"
            android:label="Add shelter list"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Project"></activity>
        <activity
            android:name=".Map_myLocation"
            android:exported="false"
            android:label="Add Map_myLocation"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Project"></activity>
        <activity
            android:name=".Shelter_search"
            android:exported="false"
            android:label="Add shelter search"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Project"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>